---
# Runner playbook for nd_manage_interface_group integration tests.
#
# Usage:
#   ansible-playbook -i tests/integration/inventory.networking \
#     tests/integration/targets/nd_manage_interface_group/run_tests.yaml \
#     -e @tests/integration/integration_config.yml

- name: ND Interface Group Integration Tests
  hosts: nd
  gather_facts: false
  roles:
    - role: "{{ playbook_dir }}"
